name: PhpUnit
on:
  push:
    branches:
      - main

  pull_request:
    branches:
      - main

jobs:
  phpunit:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6

      - run: docker compose --rm phpunit --coverage-text
